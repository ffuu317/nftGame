const { NFTSDK } = require('@your-team/nft-sdk');

const sdk = new NFTSDK({
  contractAddress: contract.address, // 后端提供
  abi: contract.abi,              // 后端提供
  rpcUrl: 'https://sepolia.infura.io/v3/1753e902a5d243499b272f4f7309ab87'    // 后端提供
});

// 获取NFT图片
const imageUrl = await sdk.getNFTImageUrl(123);
console.log(imageUrl);